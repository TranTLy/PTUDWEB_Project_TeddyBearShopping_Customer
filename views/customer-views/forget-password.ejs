<%include ../components/header%>
<link rel="stylesheet" href="stylesheets/signup.css" />
<!-- short -->
<div class="using-border py-3">
  <div class="inner_breadcrumb  ml-4">
    <ul class="short_ls">
      <li>
        <a href="index.html">Trang chủ</a>
        <span>/ /</span>
      </li>
      <li><a href="forget-password">Quên mật khẩu</a></li>
    </ul>
  </div>
</div>
<!-- //short-->

<form class="form-group from-signup" method="POST" action="forget-password">
  <label id="lable-sign-up" for="email">Quên mật khẩu</label>
  <br />
  <div class="label-input-signup ">Email đăng nhập<span>*</span></div>
  <input
    type="email"
    class="form-control required"
    name="email"
    id="email"
    aria-describedby="helpId"
    required
    onkeydown="checkEmail()"
  />

  <button
    id="btn-submit-email"
    type="submit"
  >
    Gửi mã xác nhận
  </button>
  <small></small>
</from>
<script>
 checkEmail = ()=>{
            const email = document.getElementById("email-user").value;
            console.log("email: ", email);
            $.ajax({
               url: '/checkEmail?email='+ email,
               method: "post",
               contentType: "application/json; charset=utf-8",
          dataType: "json"
            })
            .done(function(result) {
            console.log("check email ajax", result);
            if (!result.success){
               document.getElementById("check-email-message").innerHTML = "*Email chưa chính xác";
            } else{
               document.getElementById("check-email-message").innerHTML = "";
            }
          })
          .fail(function(err) {
            console.log("fail", success);
          });
         }
</script>
<% include ../components/footer%>
